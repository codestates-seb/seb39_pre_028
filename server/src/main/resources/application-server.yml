spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: admin
    password: 12345678
    url: jdbc:mysql://database-1.ciyih17rti9c.ap-northeast-2.rds.amazonaws.com/StackOverFlow?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
config:
  domain: http://{AWS S3 Endpoint}